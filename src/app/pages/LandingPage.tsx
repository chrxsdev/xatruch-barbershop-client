import { AppLayout } from '../layout/AppLayout';
import { HomeView } from '../views/HomeView';

export const LandingPage = () => {
  return (
    <AppLayout>
      <HomeView />
    </AppLayout>
  );
};
